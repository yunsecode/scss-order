export interface Config {
    orderList: string[];
    changeOnSave: boolean;
    showErrorMessages: boolean;
    autoFormat: boolean;
    tabSize: number;
    spaceBeforeClass: boolean;
    // 그냥 클래스, :hover 이런 순서
}
